<%= form_for(current_user.relationships.find_by(followed_id: interest),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Not Interested", class: "b-color" %>
<% end %>