<script>
$('.sign_showme').on('click', function(e) {
  e.preventDefault();

  $('<div id="dialog-form"></div>').dialog({
        autoOpen: true,
        width: 480,
        modal: true,
        open: function() {
            $(this).append($("<%= escape_javascript(render(:partial => 'layouts/sign_up')) %>"));
        },
        close: function() {
        $('#dialog-form').remove();
      }
    });
    dialog_form.dialog('open');
    e.preventDefault();
  });
</script>