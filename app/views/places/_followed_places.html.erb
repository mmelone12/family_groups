<% if place.image_path.blank? %>
    <%= image_tag place.uploader_image size: "50", class: "followed_items" %>
<% else %>
	<div class="img-wrap1">
    <%= image_tag place.image_path, size: "50", class: "followed_items" %>
    	<div class="img-overlay1">
    		<p><%= place.name.truncate(25) %></p>
    	</div>
    </div>
<% end %>