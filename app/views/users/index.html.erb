<% title "Family Groups - Users" %>
<% description "Find other users in the Family Groups community. Check out events for kids and family-friendly places." %>
<%= render 'layouts/menu_and_tabs' %>

    <div class="container">
      <div class="sixteen columns">
        <hr class="line slogan" />
      </div>
    </div>
<% @users.each do |user| %>
  <%= render partial: 'users/user', locals: {:user => user} %>
<% end %>

<%= render 'layouts/footer' %>